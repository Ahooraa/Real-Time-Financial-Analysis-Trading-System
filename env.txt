1.create a .env file.
2. copy and paste the following lines into .env file


# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=kafka-broker:9092
KAFKA_TOPIC=my_topic           #change this
# QuestDB Configuration
QUESTDB_HOST=localhost
QUESTDB_PORT=8812
QUESTDB_DB=qdb
QUESTDB_USER=         #replace with user
QUESTDB_PASSWORD=    #replace with password
